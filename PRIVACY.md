# Privacy

Localhost QA sends browser requests only to the explicitly supplied test origin. Localhost is the default and non-local origins require exact per-session authorization. Screenshots and reports stay local, are redacted for common secrets, and are never uploaded by the plugin. Use synthetic fixture credentials only.
