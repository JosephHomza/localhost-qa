import fs from"node:fs";import path from"node:path";import{fileURLToPath}from"node:url";const root=path.resolve(path.dirname(fileURLToPath(import.meta.url)),"..");const manifest=JSON.parse(fs.readFileSync(path.join(root,".codex-plugin","plugin.json"),"utf8"));if(manifest.name!=="localhost-qa"||!manifest.skills||!manifest.interface?.displayName)throw new Error("invalid plugin manifest");for(const skill of["run-qa","generate-regression"]){const text=fs.readFileSync(path.join(root,"skills",skill,"SKILL.md"),"utf8");if(!text.startsWith("---\n")||!text.includes("description:"))throw new Error(`invalid skill ${skill}`);}console.log("Plugin manifest and skill metadata validation passed.");
