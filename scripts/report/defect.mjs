import crypto from"node:crypto";
export function severityFor({blocksCoreFlow=false,degrades=false}={}){return blocksCoreFlow?"high":degrades?"medium":"low";}
export function createDefect(input){if(input.flakyChecked!==true||!/^[1-9]\d*\/[1-9]\d*$/.test(input.reproducibleRuns??""))throw new Error("defect requires completed flaky checking");const[a,b]=input.reproducibleRuns.split("/").map(Number);if(a!==b)throw new Error("flaky failure cannot be emitted as a confirmed defect");return{defectId:crypto.randomUUID(),title:input.title,severity:severityFor(input),origin:input.origin,viewport:input.viewport,steps:input.steps,evidence:input.evidence??{},flakyChecked:true,reproducibleRuns:input.reproducibleRuns,notProductionTested:new URL(input.origin).hostname==="localhost"||["127.0.0.1","::1"].includes(new URL(input.origin).hostname)};}
