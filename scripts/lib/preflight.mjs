import fs from"node:fs";import path from"node:path";import{fileURLToPath}from"node:url";import{emit,result}from"./report.mjs";
export async function preflight(){const findings=[];try{await import("playwright");findings.push({severity:"info",title:"Playwright package",message:"installed"});}catch{findings.push({severity:"error",title:"Playwright package missing",message:"Run npm.cmd install in the plugin directory."});}const home=process.env.PLAYWRIGHT_BROWSERS_PATH||path.join(process.env.LOCALAPPDATA??"", "ms-playwright");if(!fs.existsSync(home))findings.push({severity:"warning",title:"Browser binary not detected",message:"Run npx.cmd playwright install chromium."});return result({operation:"preflight",status:findings.some(x=>x.severity==="error")?"blocked":"ok",findings});}
if(process.argv[1]&&path.resolve(process.argv[1])===fileURLToPath(import.meta.url))emit(await preflight());
