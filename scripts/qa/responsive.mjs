export async function responsiveFindings(page){return page.evaluate(()=>{const overflow=document.documentElement.scrollWidth>document.documentElement.clientWidth;const offenders=[...document.querySelectorAll("body *")].filter(el=>el.getBoundingClientRect().right>document.documentElement.clientWidth+1).slice(0,10).map(el=>el.tagName.toLowerCase());return overflow?[{type:"horizontal-overflow",offenders}]:[];});}
