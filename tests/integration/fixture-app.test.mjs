import assert from"node:assert/strict";import http from"node:http";import test from"node:test";import{probe}from"../../scripts/qa/detect-servers.mjs";
test("server probe reports a reachable local fixture and framework",async(t)=>{const server=http.createServer((req,res)=>{res.setHeader("content-type","text/html");res.end("<html><script src='/@vite/client'></script></html>");});await new Promise(resolve=>server.listen(0,"127.0.0.1",resolve));t.after(()=>server.close());const address=server.address();const found=await probe(`http://127.0.0.1:${address.port}`);assert.equal(found.reachable,true);assert.equal(found.framework,"Vite");});
